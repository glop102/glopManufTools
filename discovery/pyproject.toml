[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "discovery"
version = "0.1.0"
dependencies = ["pydantic", "scapy"]

[tool.hatch.build.targets.wheel]
packages = ["discovery"]
